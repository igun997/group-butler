node_modules/
.next/
dist/
coverage/
*.tsbuildinfo

# env files: examples are committed, real values never are
.env
.env.*
!.env.example
!apps/web/.env.production.example
!apps/worker/.env.production.example

# whatsmeow auth state (linked-device keys) and local worker build output
apps/worker/.localdata/
apps/worker/whatsapp-worker
# `go build ./...` names the binary after the package directory.
apps/worker/worker
